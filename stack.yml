version: "3.8"

x-claude-base: &claude-base
  image: lumir/claude:latest
  networks:
    - default
  deploy:
    replicas: 1
    restart_policy:
      condition: any

services:
  alice:
    <<: *claude-base
    hostname: alice
    environment:
      SSH_PUBLIC_KEY: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI...replace-with-alice-key... alice"
    ports:
      - target: 22
        published: 2201
        protocol: tcp
        mode: host
    volumes:
      - alice-claude:/home/claude/.claude
      - alice-history:/commandhistory
      - alice-workspace:/workspace

  bob:
    <<: *claude-base
    hostname: bob
    environment:
      SSH_PUBLIC_KEY: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI...replace-with-bob-key... bob"
    ports:
      - target: 22
        published: 2202
        protocol: tcp
        mode: host
    volumes:
      - bob-claude:/home/claude/.claude
      - bob-history:/commandhistory
      - bob-workspace:/workspace

volumes:
  alice-claude:
  alice-history:
  alice-workspace:
  bob-claude:
  bob-history:
  bob-workspace:

networks:
  default:
    driver: overlay
